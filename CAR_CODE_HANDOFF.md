# car_code 项目交接文档

更新时间：2026-07-24  
用途：供新的 Codex 对话快速接手本项目，继续进行实车测试、定位问题和后续优化。

## 1. 当前目标与状态

本工程用于完成 2024 年全国大学生电子设计竞赛赛区赛 H 题“自动行驶小车”，主控为立创·天猛星 MSPM0G3507 开发板。

当前用户实车状态：

- 天猛星 MSPM0G3507 开发板已经装到自制小车 PCB 上。
- 八路数字灰度传感器已经接入并能正常工作。
- OLED 曾出现上电后不显示、按 MCU Reset 后恢复的问题；恢复后测得供电约 3.2～3.3 V。
- 左右电机已经接入并测试正常。
- 题目第 1、2 问已经能够实现。
- 下一步是理解并实测第 3 问的 8 字路线。
- 本对话没有主动修改工程源代码。由于当前环境没有可用的 `git` 命令，无法用 `git status` 证明工作树是否原本干净；不要把工程里已有变化都归因于本对话。

## 2. 资料来源总表

### 2.1 当前代码工程

工程根目录：

`D:\slz\TIM0G3507\TIM0G3507\car_code\car_code`

重要目录：

- `user\`：小车应用层、模式逻辑、循迹、编码器、IMU、按键、声光提示。
- `ml_libs\`：GPIO、定时器、PWM、电机、UART、OLED、I2C 等底层封装。
- `m0_sdk\`：工程内附带的 TI MSPM0 SDK、CMSIS 和 DriverLib。
- `code\`：另一份 PID 源文件，当前主要控制逻辑仍集中在 `user\`。
- `user\project.uvprojx`：Keil 工程文件。
- `user\Objects\project.hex`：现有编译产物，不能假定一定与当前全部源码严格同步，重新下载前应重新编译。

重点源文件：

- `user\main.c`：系统初始化、OLED显示、模式选择、10 ms 控制调度。
- `user\xunji.c`：第1～4问的主要路线逻辑。
- `user\encoder.c`：左右正交编码器外部中断计数。
- `user\IMU.c`：JY61P/WT 系列 0x55 协议角度帧解析。
- `user\pid_angle.c`：当前实际使用的航向 PD。
- `user\_pid.c`：通用位置式/增量式 PID 框架；其中大量速度闭环代码目前被注释。
- `ml_libs\ml_motor.c`：TB6612方向控制和左右电机PWM接口。
- `ml_libs\ml_pwm.c`：PWM初始化与占空比更新。
- `ml_libs\ml_oled.c`、`ml_libs\ml_oled.h`：软件模拟I2C OLED驱动。
- `user\buzzer_light.c`：蜂鸣器和PCB外接LED。

### 2.2 题目原文

本地文件：

`D:\slz\实验\选题\H题_自动行驶小车.pdf`

网络镜像：

<https://res.eeyxs.com/21dianyuan/nuedc/202410/20241009092953S1GDHZ.pdf>

题目核心要求：

1. A→B直行并停车，声光提示，15 s以内。
2. A→B→沿弧到C→C→D→沿弧到A，30 s以内。
3. A→C→沿弧到B→B→D→沿弧到A，形成8字路线，40 s以内。
4. 按第3问路线行驶4圈。

题目说明还包括：只能前进、不得使用履带和麦克纳姆轮、主控必须为 TI MSPM0 系列、不得安装摄像头。

辅助理解第3/4问路线的公开资料：

- <https://weigo6.github.io/suffine-wiki/notes/24%E7%94%B5%E8%B5%9BH/>
- <https://www.cdhyyds.com/document/2024_TI_H_auto_car.html>

### 2.3 模块资料集合

根目录：

`D:\slz\模块资料集合(1)\模块资料集合`

已有资料：

- `0.96寸OLED显示屏模块\0.96寸OLED显示屏4脚 新款.pdf`
- `0.96寸OLED显示屏模块\OLED使用说明.txt`
- `8路数字量灰度传感\八路灰度传感参考资料.txt`
- `8路数字量灰度传感\微信图片_20260721124438_324_47.png`
- `8路数字量灰度传感\微信图片_20260721124438_325_47.png`
- `八路灰度传感器文本文件(1)\文本文件\第五代数字量智能灰度传感器说明书.pdf`
- `八路灰度传感器文本文件(1)\文本文件\接线说明.txt`
- `八路灰度传感器文本文件(1)\文本文件\关于IIC通信注意事项.txt`
- `八路灰度传感器文本文件(1)\文本文件\灰度计算表.xls`
- `JY61P陀螺仪模块\JY61P模块资料.txt`
- `TB6612电机驱动模块\TB6612FNG电机驱动模块.pdf`
- `TB6612电机驱动模块\TB6612FNG电机驱动模块蓝板.pdf`
- `TB6612电机驱动模块\TB6612相关教程.txt`
- `MP1584EN 直流降压模块\接线说明.png`
- `DAPLINK仿真器带壳\使用说明.txt`
- `LED红灯\产品参数.png`
- `小车加高板\`中的两张安装图片。

资料中的外部链接：

- OLED说明：<https://www.bilibili.com/opus/1019499397834080257>
- JY61P资料：<https://wit-motion.yuque.com/wumwnr/docs/np25sf?singleDoc>
- 八路灰度调试视频：<https://v.youku.com/v_show/id_XNjM3MzUxNDIzMg==.html>
- TB6612说明：<https://www.bilibili.com/opus/1011818170973945897>

OLED资料特别提示：如果显示乱码，可尝试将 SDA、SCL 分别用 10 kΩ 电阻上拉到 VCC。当前工程是软件模拟I2C，代码本身不读取 ACK。

工程内已存在 OLED PDF 的页面渲染：

`tmp\pdfs\oled-module\page-01.png` 至 `page-34.png`

### 2.4 器件清单

本地文件：

`C:\Users\qmsun\Downloads\器件清单.pdf`

该文件曾用于核对当年比赛器材范围。新对话如需引用“是否明确允许/列出灰度传感器”等具体措辞，应重新打开原文确认，不要仅凭本交接文档转述。

### 2.5 PCB原理图和手写引脚图

用户提供过 PCB 总原理图截图和手写引脚分配图。原始截图位于系统临时目录，路径可能随清理失效：

- `C:\Users\qmsun\AppData\Local\Temp\codex-clipboard-0e4eee29-38f3-4c90-be21-af9c96801f18.png`
- `C:\Users\qmsun\AppData\Local\Temp\codex-clipboard-1ce2437f-82f5-4b4b-a92c-62d95035c324.png`
- `C:\Users\qmsun\AppData\Local\Temp\codex-clipboard-1fdee6e7-f19f-4405-9b23-2b6c54253685.png`

已从图中人工核对出的接线结论见下一节。若要修改 PCB 或追查硬件故障，应让用户重新提供原理图源文件或高清导出图，不要只依赖临时截图。

### 2.6 天猛星官方资料

板载绿色用户LED确认来自立创开发板官方文档：

- <https://wiki.lckfb.com/zh-hans/tmx-mspm0g3507/ccs-beginner/led.html>
- <https://wiki.lckfb.com/zh-hans/tmx-mspm0g3507/keil-beginner/led.html>

确认结果：

- 板载用户LED：PB22。
- PB22输出高电平时点亮。
- 这与小车PCB上的外接LED PB26不是同一颗灯。

## 3. 已确认的硬件接线

### 3.1 TB6612与电机

| 功能 | MCU引脚 | 说明 |
|---|---|---|
| PWMA | PB6 | 左轮PWM，TIMG6 CC0 |
| PWMB | PB7 | 右轮PWM，TIMG6 CC1 |
| 左轮方向 | PB24、PB20 | 对应TB6612 A通道方向输入 |
| 右轮方向 | PB19、PB18 | 对应TB6612 B通道方向输入 |
| STBY | PB16 | 初始化后置高使能 |
| VM | +12V | 电机功率电源 |
| VCC | +5V | TB6612逻辑电源 |

`Set_left_pwm()` 和 `Set_right_pwm()` 的参数正负号用于选择方向，绝对值用于设置占空比。

### 3.2 编码器

| 编码器 | A相 | B相 |
|---|---|---|
| 左轮 | PA12 | PA13 |
| 右轮 | PA24 | PA15 |

右轮接线以红色修正后的 `PA24/PA15` 为准，不使用手写图中被划掉的旧顺序。

### 3.3 八路数字灰度

| 通道 | MCU引脚 |
|---|---|
| D1 | PB13 |
| D2 | PB15 |
| D3 | PA31 |
| D4 | PA28 |
| D5 | PB1 |
| D6 | PB4 |
| D7 | PB17 |
| D8 | PB12 |

代码中对数字量做了反相解释：GPIO读到低电平时 `digtal()` 返回1，GPIO读到高电平时返回0。实际调试时不要只根据变量名猜黑白关系，应结合OLED/调试器和实物确认。

### 3.4 OLED

| OLED | PCB/MCU |
|---|---|
| GND | GND |
| VCC | 3.3V |
| SCL | PB2 |
| SDA | PB3 |

当前驱动：

- 4针I2C OLED。
- 软件模拟I2C。
- 写地址发送 `0x78`，对应7位地址 `0x3C`。
- 初始化序列是典型 SSD1306 风格。
- 没有ACK检查，OLED不响应时软件无法直接报错。

### 3.5 JY61P陀螺仪

| 功能 | MCU引脚 |
|---|---|
| MCU UART1 TX | PA8 |
| MCU UART1 RX | PA9 |

代码以 9600 baud 接收 `0x55 0x53` 角度帧，解析 Z轴偏航角到 `yaw_angle_int`。

注意：`imu_init()` 只初始化 UART，没有在代码中主动发送“航向清零”命令，也没有软件零点偏移。程序中的 `-36°`、`-139°` 等是固定目标角，因此实车启动姿态和陀螺仪零点非常关键。

### 3.6 其他

| 功能 | MCU引脚 |
|---|---|
| mode按键 | PA17 |
| set/启动按键 | PA27 |
| 蜂鸣器 | PB8 |
| 小车PCB外接LED | PB26 |
| 天猛星板载绿色LED | PB22 |

PCB总原理图表达的是“底板内部铜线如何把MCU引脚接到驱动器和插座”。开发板插到底板后，不需要再用杜邦线重复连接 PB6、PB7 等内部信号；电机、编码器、OLED、灰度、陀螺仪和电源仍需插到对应外部接口。

## 4. 软件架构与运行流程

### 4.1 初始化

`user\main.c` 顺序大致为：

1. `system_init()`：系统时钟约80 MHz，GPIO和SysTick初始化。
2. 延时500 ms等待供电稳定。
3. `OLED_Init()`。
4. `imu_init()`：UART1 9600。
5. `key_init()`。
6. `motor_init()`。
7. `encoder_init()`。
8. `uart0_init()`。
9. `xunji_init()`。
10. `buzzer_light_init()`。
11. 初始化 TIMG8 每10 ms中断。

主循环负责：

- 解析陀螺仪数据。
- 读取mode/set按键。
- OLED显示 `mode`、`set`、`yaw`、`pid_out`。

TIMG8中断每10 ms：

- 读取并清零左右编码器计数。
- 根据 `mode` 和 `set` 调用 `track1()`～`track4()`。

### 4.2 PWM

- PWM定时器：TIMG6。
- 左轮：CC0/PB6。
- 右轮：CC1/PB7。
- PWM频率：1000 Hz。
- `MAX_DUTY = 10000`。
- `4000`代表约40%占空比，`5000`代表约50%。
- `pwm_update()`会将大于10000的值限制为10000。

PWM只是执行量；开环或闭环取决于是否使用反馈修正PWM。

### 4.3 当前闭环情况

当前工程是混合控制：

- 电机轮速：主要是开环。编码器每10 ms被读取，但当前有效路线代码没有把编码器计数送入速度PID。
- 航向：部分直线路段使用 JY61P 航向角 PD 闭环。
- 圆弧循迹：根据八路灰度组合查表，直接选择左右PWM，属于离散反馈控制，不是连续误差PID。

`user\pid_angle.c` 当前参数：

- `p = 70`
- `d = 15`
- `pid_out`限制为 `-2000～2000`

计算形式：

`pid_out = p * err + d * (err - err_last)`

通用速度PID框架位于 `user\_pid.c`，但 `main.c` 中左右电机PID初始化以及速度闭环调用目前被注释，不能把当前程序称为“已启用编码器速度闭环”。

## 5. 四个模式的理解

### 5.1 mode 1 / track1

目标：A→B直行，遇到终点黑线后停车并声光提示。

- 航向目标约0°。
- 基础PWM约4000。
- 使用 `pid_angle(0, yaw_angle_int)`做直线航向修正。
- 终点依赖灰度检测。

### 5.2 mode 2 / track2

目标：A→B→弧线到C→C→D→弧线到A。

- A→B：航向目标0°。
- C→D：航向目标约-175°。
- 两个半圆弧通过八路灰度查表控制。
- `change_flag1`通过“全白/检测到黑线”状态切换累计经过的端点。
- `change_flag1 == 4`时停车。

用户已确认第1、2问实车可以实现。

### 5.3 mode 3 / track3

题目路线：

`A → C → 沿弧到B → B → D → 沿弧到A → 停车`

核心状态：

| `change_flag1` | 路段 | 控制 |
|---:|---|---|
| 0 | A→C | 航向目标-36°，基础PWM3000 |
| 1 | C→B弧线 | 八路灰度查表循迹 |
| 2 | B→D | 航向目标-139°，基础PWM4000 |
| 3 | D→A弧线 | 八路灰度查表循迹 |
| 4 | 到达A | 左右PWM置0 |

第一次运行时还会：

```c
Set_right_pwm(-1000);
Set_left_pwm(1000);
delay_ms(100);
```

用于给转向一个短暂的原地差速启动动作，随后进入-36°航向控制。

第3问对初始摆放的明确假设：

- 小车位于A点。
- 车头初始朝A→B，而不是直接朝C。
- 此时OLED `yaw`应接近0°。
- 八路灰度初始应全部处在白区，避免一启动就把 `change_flag1`加到1。
- 启动后程序从0°转向约-36°，对应A→C。
- 如果一开始已经朝向C，程序仍会再转-36°，路线会错误。
- 每次单独测试第3问前最好先Reset，使 `change_flag1`、`flag3`、`now_statue` 等全局状态归零。
- Reset MCU不一定会重置独立供电的JY61P；应以OLED显示的yaw为准。

推荐操作：

1. Reset或重新上电。
2. 在A点让车头朝B。
3. 确认灰度条位于白区。
4. 等待陀螺仪稳定，确认yaw接近0。
5. 按mode键直到OLED显示3。
6. 再按set启动。

### 5.4 mode 4 / track4

目标：按第3问8字路线连续4圈。

- 依靠同一个 `change_flag1`累计16次黑/白状态切换。
- 每圈开始处都有不同的强制转向延时。
- 各圈写死的航向目标逐圈变化，用于补偿实车累计漂移：
  - 第1圈约 `-37° / -139°`
  - 第2圈约 `-32° / -136°`
  - 第3圈约 `-30° / -132°`
  - 第4圈约 `-26° / -125°`
- `change_flag1 == 16`时停车。

这些角度明显属于原车、原场地的经验参数，换底盘、轮胎、陀螺仪安装方向或场地尺寸后很可能需要重新标定。

## 6. 已发现的风险和潜在问题

以下内容没有在本对话中修改，仅作为后续审查重点。

### 6.1 航向目标使用固定绝对角

`pid_angle()`直接使用 `yaw_angle_int`，没有“启动时yaw作为零点”的软件偏移。只要陀螺仪初值不是预期的0°，第2～4问的固定目标角都会整体偏移。

建议后续优先考虑：

- 启动时记录 `yaw_zero`；
- 后续统一使用相对航向；
- 正确处理 ±180°/±360°环绕。

### 6.2 编码器速度闭环未启用

编码器读取已经存在，但有效路线逻辑没有用它稳定左右轮速度。电池电压、载荷和左右电机差异仍会影响直线与转向。

### 6.3 通用 `turn_pid()`疑似对象名错误

`user\_pid.c` 中给 `angle.now/target`赋值，却调用 `pid_cal(&Angle)`。`angle`和`Angle`是两个不同对象。该函数目前不是主要有效路径，但后续若重新启用必须先核对。

### 6.4 PID初始化被注释

`main.c` 中 `pid_init(&motorA...)`、`pid_init(&motorB...)`、`pid_init(&Angle...)`均被注释。当前真正工作的航向修正是独立的 `pid_angle()`。

### 6.5 在10 ms定时器中断里阻塞延时

`track3()`在中断上下文中调用 `delay_ms(100)`；`track4()`甚至有350 ms、500 ms等组合延时。这样会长时间阻塞中断处理，影响编码器采样、串口和整体实时性。

后续优化应改为非阻塞状态机和时间戳/计数器。

### 6.6 灰度状态判断重复读取且使用位运算符

代码大量重复调用 `D1`～`D8`宏，每个宏都会再次读取GPIO。部分条件使用单竖线 `|`，不是逻辑或 `||`。在当前0/1表达式下通常仍能得到结果，但没有短路求值，并且某些表达式存在运算优先级可读性风险。

建议每10 ms先一次性采样8路，保存成位图，再做状态判断。

### 6.7 终点/端点完全依赖黑白跳变计数

如果初始灰度压在线上、线宽/高度变化造成瞬间全白、传感器抖动或一次端点被重复识别，`change_flag1`就会错位，后续整条路线阶段都会错误。

应考虑消抖、最小阶段持续时间、编码器距离门限或状态锁。

### 6.8 OLED没有ACK和总线恢复

软件I2C发送后不检查OLED是否应答，也没有SDA被拉低时的总线恢复流程。OLED上电时序不稳定时，按Reset后恢复的现象与“主控初始化发生在OLED电源稳定之前”或“I2C总线初始状态异常”相符，但目前尚未定论。

若问题复现，建议：

- 增加OLED上电等待；
- 初始化前发送9个SCL恢复时钟；
- 检查SDA/SCL上拉；
- 测量PB2/PB3波形；
- 只连接OLED做最小测试。

### 6.9 电机干扰与供电

如果以后出现电机启动导致OLED熄灭、MCU复位或灰度乱跳，应优先测电机启动瞬间的12 V、5 V和3.3 V，并检查：

- 电池和降压模块供电能力；
- 公共地；
- 插头/排线压降；
- 电机端抑制电容；
- 驱动和逻辑电源去耦；
- USB与外部电源是否回灌。

## 7. 已完成的硬件排查经验

### 7.1 板载LED

只有天猛星开发板时，可用PB22测试板载绿色LED：

```c
system_init();
gpio_init(GPIOB, DL_GPIO_PIN_22, PB22, OUT);
gpio_set(GPIOB, DL_GPIO_PIN_22, 1);  // 亮
```

小车PCB外接LED是PB26，两者不要混淆。

### 7.2 OLED曾经不显示

现象：

- OLED第一次曾亮过，之后上电不亮。
- 曾测得插OLED后约2.96 V，拔掉后3.3 V。
- 后来上电按Reset后正常显示，重新测量约3.2～3.3 V。

当前判断：

- 没有证据证明OLED烧毁。
- 之前2.96 V可能是测量或接触问题，也可能是当时供电尚未稳定。
- “按Reset后恢复”更值得关注上电时序、I2C初态和复位先后关系。
- 若再次出现，分别测开发板3V3引脚和OLED自身VCC，区分整个电源轨下跌还是连接压降。

### 7.3 电机

- 用户已完成左右电机测试并确认正常。
- 初次测试采用低PWM、轮子悬空、单轮正反转的方式。
- 后续仍需单独确认编码器计数的左右对应和正负方向。

## 8. 建议下一对话优先执行的工作

1. 先不改算法，按本文件第5.3节的摆放方式实测第3问。
2. 记录启动前OLED yaw、A→C实际方向、到C/B/D/A时的 `change_flag1`。
3. 若A→C方向偏差固定，先判断是初始yaw不为0还是目标角-36°不适合当前场地。
4. 若进弧线失败，观察到C时哪一路灰度先压线，并核对强制转向方向。
5. 用Keil Watch或OLED临时显示左右编码器10 ms计数，确认编码器实际可用。
6. 每次切换题目前Reset，避免上一模式遗留状态。
7. 在实车数据明确前，不要一次性重构全部路线逻辑。
8. 后续正式优化优先级建议：
   - 启动航向相对零点；
   - 非阻塞状态机；
   - 灰度一次采样和消抖；
   - 编码器速度闭环；
   - OLED上电与I2C恢复；
   - 参数配置化，减少写死角度/PWM。

## 9. 对下一位助手的提醒

- 用户此前明确要求过“暂时不要修改代码”；除非新对话明确授权修改，先以解释、测量和诊断为主。
- 用户现在已经进入实车调试阶段，安全建议应包括轮子悬空、低PWM、随时断电、禁止带电插拔电机/编码器。
- 结论必须区分：
  - 原理图/源码明确事实；
  - 资料说明；
  - 根据代码和现象作出的推断；
  - 尚需实车验证的参数。
- 临时目录中的原理图截图可能消失；涉及PCB修改前应让用户重新上传高清原理图。
- 代码中不少中文注释存在乱码，但不影响C语句本身。读取时应以函数和引脚定义为准。

